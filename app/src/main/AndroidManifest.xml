<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tieorange.edu.hellmanager">

    <application
        android:name=".HellManagerApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.AddTortureDepActivity" />
        <activity
            android:name=".Activity.TortureDepsActivity"
            android:label="@string/title_activity_torture_dep"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DepartmentActivity" />
        <activity
            android:name=".Activity.DepartmentTabsActivity"
            android:label="@string/title_activity_department_tabs"
            android:parentActivityName=".Activity.TortureDepsActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Activity.TortureDepsActivity" />
        </activity>
        <activity
            android:name=".Activity.AddPunishmentToolActivity"
            android:label="Add punishment tool" />
        <activity android:name=".Activity.AddSinnerActivity"></activity>
    </application>

</manifest>